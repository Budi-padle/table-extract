# table-extract

Extract tables from PDFs and export to CSV/JSON.

## Features

- Detect table boundaries using line detection
- Extract cell content with OCR
- Export to CSV, JSON, or pandas DataFrame
